<%= render '/shared/subheader' %>
<div class="container">
  <div class="row">

    <div class="col-sm-8">
      <h1>Recent blog posts</h1>
      <% @posts.each do |post| %>
        <%=render '/static_pages/recent_posts', post: post %>
          <% end %>
    </div>
    
    <div class="col-sm-4">
      <%= render '/shared/sidebar' %>
    </div>

  </div>
</div>
